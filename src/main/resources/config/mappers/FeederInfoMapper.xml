<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- user_info表 -->
<mapper namespace="com.equestrianism.api.dao.FeederInfoMapper">

    <!--返回Map-->
    <resultMap id="BaseResultMap" type="com.equestrianism.api.model.po.FeederInfoEntity">
        <id column="feeder_id" property="feederId" jdbcType="VARCHAR"/>
        <result column="feeder_name" property="feederName" jdbcType="VARCHAR"/>
        <result column="sex" property="sex" jdbcType="INTEGER"/>
        <result column="skill_desc" property="skillDesc" jdbcType="VARCHAR"/>
        <result column="horse_id" property="horseId" jdbcType="VARCHAR"/>
        <result column="hire_date" property="hireDate" jdbcType="VARCHAR"/>
        <result column="id_card_image" property="idCardImage" jdbcType="BLOB"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="delete_flag" property="deleteFlag" jdbcType="INTEGER"/>
        <result column="data_version" property="dataVersion" jdbcType="INTEGER"/>
        <result column="create_date" property="createDate" jdbcType="TIMESTAMP"/>
        <result column="update_date" property="updateDate" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--基本的sql查询字段 公共引用...-->
    <sql id="Base_Column_List">
        feeder_id, feeder_name, sex, skill_desc, horse_id, hire_date, id_card_image, status,
        delete_flag, data_version, create_date, update_date
    </sql>

    <insert id="insert" parameterType="com.equestrianism.api.model.po.FeederInfoEntity">
        INSERT INTO
            feeder_info (
                feeder_id,
                feeder_name,
                sex,
                skill_desc,
                horse_id,
                hire_date,
                id_card_image,
                status,
                delete_flag,
                data_version,
                create_date
            ) VALUES (
                #{feederId, jdbcType=VARCHAR},
                #{feederName, jdbcType=VARCHAR},
                #{sex, jdbcType=INTEGER},
                #{skillDesc, jdbcType=VARCHAR},
                #{horseId, jdbcType=VARCHAR},
                #{hireDate, jdbcType=VARCHAR},
                #{idCardImage, jdbcType=BLOB},
                1,
                0,
                1,
                now()
            )
    </insert>

    <update id="updateBySelective" parameterType="com.equestrianism.api.model.po.FeederInfoEntity">
        UPDATE
            feeder_info
        <set>
            <if test="feederName != null and feederName != ''">
                feeder_name = #{feederName, jdbcType=VARCHAR},
            </if>
            <if test="sex != null">
                sex = #{sex, jdbcType=INTEGER},
            </if>
            <if test="skillDesc != null and skillDesc != ''">
                skill_desc = #{skillDesc, jdbcType=INTEGER},
            </if>
            <if test="horseId != null and horseId != ''">
                horse_id = #{horseId, jdbcType=VARCHAR},
            </if>
            <if test="hireDate != null and hireDate != ''">
                hire_date = #{hireDate, jdbcType=VARCHAR},
            </if>
            <if test="idCardImage != null and idCardImage != ''">
                id_card_image = #{idCardImage, jdbcType=BLOB},
            </if>
            <if test="status != null">
                status = #{status, jdbcType=INTEGER},
            </if>
            <if test="deleteFlag != null">
                delete_flag = #{deleteFlag, jdbcType=INTEGER}
            </if>
        </set>
        WHERE
            feeder_id = #{feederId, jdbcType=VARCHAR}
    </update>

    <select id="selectFeederListByPage" parameterType="com.equestrianism.api.model.vo.feeder_info.FeederInfoListVO"
            resultType="com.equestrianism.api.model.model.FeederInfoListModel">
        SELECT
            a.feeder_id       feederId,
            a.feeder_name     feederName,
            a.sex             sex,
            a.id_card_image   idCardImage,
            a.skill_desc      skillDesc,
            a.hire_date       hireDate,
            b.horse_name      horseName
        FROM
            feeder_info a
        LEFT JOIN
            horse_info b
        ON
            a.horse_id = b.horse_id
        WHERE
            a.delete_flag = 0
        <if test="feederName != null and feederName != ''">
            AND
                a.feeder_name like CONCAT('%',#{feederName, jdbcType=VARCHAR},'%')
        </if>
        limit #{beginIndex}, #{pageRecorders}
    </select>

    <select id="countFeederListByPage" parameterType="com.equestrianism.api.model.vo.feeder_info.FeederInfoListVO"
            resultType="java.lang.Integer">
        SELECT
            count(1)
        FROM
            feeder_info a
        LEFT JOIN
            horse_info b
        ON
            a.horse_id = b.horse_id
        WHERE
            a.delete_flag = 0
        <if test="feederName != null and feederName != ''">
            AND
                a.feeder_name like CONCAT('%',#{feederName, jdbcType=VARCHAR},'%')
        </if>
    </select>

    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        SELECT
            <include refid="Base_Column_List"/>
        FROM
            dictionary_info
        WHERE
            dictionary_id = #{dictionaryId, jdbcType=INTEGER}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
            <include refid="Base_Column_List"/>
        FROM
            dictionary_info
        WHERE
            status = 1
        AND
            delete_flag = 0
    </select>

    <select id="selectFeederInfoByDetail" parameterType="java.lang.String"
            resultType="com.equestrianism.api.model.bo.FeederInfoDetailBO">
        SELECT
            feeder_id           feederId,
            feeder_name         feederName,
            sex                 sex,
            skill_desc          skillDesc,
            horse_id            horseId,
            hire_date           hireDate,
            id_card_image       idCardImage
        FROM
            feeder_info
        WHERE
            feeder_id = #{feederId, jdbcType=VARCHAR}
    </select>

</mapper>
