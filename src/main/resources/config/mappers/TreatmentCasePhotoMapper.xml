<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- user_info表 -->
<mapper namespace="com.equestrianism.api.dao.TreatmentCasePhotoMapper">

    <!--返回Map-->
    <resultMap id="BaseResultMap" type="com.equestrianism.api.model.po.TreatmentCasePhotoEntity">
        <id column="case_photo_id" property="casePhotoId" jdbcType="VARCHAR"/>
        <result column="treatment_case_id" property="treatmentCaseId" jdbcType="VARCHAR"/>
        <result column="photo_type" property="photoType" jdbcType="INTEGER"/>
        <result column="photo" property="photo" jdbcType="BLOB"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="delete_flag" property="deleteFlag" jdbcType="INTEGER"/>
        <result column="data_version" property="dataVersion" jdbcType="INTEGER"/>
        <result column="create_date" property="createDate" jdbcType="TIMESTAMP"/>
        <result column="update_date" property="updateDate" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--基本的sql查询字段 公共引用...-->
    <sql id="Base_Column_List">
        case_photo_id, treatment_case_id, photo_type, photo, status, delete_flag, data_version, create_date, update_date
    </sql>

    <insert id="insert" parameterType="com.equestrianism.api.model.po.TreatmentCasePhotoEntity">
        INSERT INTO
            treatment_case_photo (
                case_photo_id,
                treatment_case_id,
                photo_type,
                photo,
                status,
                delete_flag,
                data_version,
                create_date
            ) VALUES (
                #{casePhotoId, jdbcType=VARCHAR},
                #{treatmentCaseId, jdbcType=VARCHAR},
                #{photoType, jdbcType=INTEGER},
                #{photo, jdbcType=BLOB},
                1,
                0,
                1,
                now()
            )
    </insert>

    <update id="updateBySelective" parameterType="com.equestrianism.api.model.po.TreatmentCasePhotoEntity">
        UPDATE
            treatment_case_photo
        <set>
            <if test="treatmentCaseId != null and treatmentCaseId != ''">
                treatment_case_id = #{treatmentCaseId, jdbcType=VARCHAR},
            </if>
            <if test="status != null">
                status = #{status, jdbcType=INTEGER},
            </if>
            <if test="deleteFlag != null">
                delete_flag = #{deleteFlag, jdbcType=INTEGER}
            </if>
        </set>
        WHERE
            case_photo_id = #{casePhotoId, jdbcType=VARCHAR}
    </update>

    <select id="selectByTreatmentCaseId" parameterType="com.equestrianism.api.model.vo.treatment_case.TreatmentCasePhotoListVO"
            resultType="com.equestrianism.api.model.model.TreatmentCasePhotoListModel">
        SELECT
            case_photo_id   casePhotoId,
            photo           photo
        FROM
            treatment_case_photo
        WHERE
            treatment_case_id = #{treatmentCaseId, jdbcType=VARCHAR}
        AND
            photo_type = #{photoType, jdbcType=INTEGER}
        AND
            delete_flag = 0
        AND
            status = 1
    </select>

</mapper>
