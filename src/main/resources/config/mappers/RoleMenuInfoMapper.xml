<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- user_info表 -->
<mapper namespace="com.equestrianism.api.dao.RoleMenuInfoMapper">

    <!--返回Map-->
    <resultMap id="BaseResultMap" type="com.equestrianism.api.model.po.RoleMenuInfoEntity">
        <id column="role_menu_id" property="roleMenuId" jdbcType="INTEGER"/>
        <result column="role_id" property="roleId" jdbcType="VARCHAR"/>
        <result column="menu_id" property="menuId" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="delete_flag" property="deleteFlag" jdbcType="INTEGER"/>
        <result column="data_version" property="dataVersion" jdbcType="INTEGER"/>
        <result column="create_date" property="createDate" jdbcType="TIMESTAMP"/>
        <result column="update_date" property="updateDate" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--基本的sql查询字段 公共引用...-->
    <sql id="Base_Column_List">
        role_menu_id, role_id, menu_id, status, delete_flag, data_version, create_date, update_date
    </sql>


    <insert id="insert" parameterType="com.equestrianism.api.model.po.RoleMenuInfoEntity">
        INSERT INTO
            role_menu_info (
                role_id,
                menu_id,
                status,
                delete_flag,
                data_version,
                create_date
            ) VALUES (
                #{roleId, jdbcType=VARCHAR},
                #{menuId, jdbcType=INTEGER},
                1,
                0,
                1,
                now()
            )
    </insert>

    <update id="updateBySelective" parameterType="com.equestrianism.api.model.po.RoleMenuInfoEntity">
        UPDATE
            role_menu_info
        <set>
            <if test="deleteFlag != null">
                delete_flag = #{deleteFlag, jdbcType=INTEGER},
            </if>
            <if test="status != null">
                status = #{status, jdbcType=INTEGER}
            </if>
        </set>
        WHERE
            role_id = #{roleId, jdbcType=VARCHAR}
        AND
            menu_id = #{menuId, jdbcType=INTEGER}
    </update>

    <select id="selectByRoleId" parameterType="java.lang.String" resultType="java.lang.Integer">
        SELECT
            menu_id
        FROM
            role_menu_info
        WHERE
            role_id = #{roleId, jdbcType=VARCHAR}
        AND
            status = 1
        AND
            delete_flag = 0
    </select>

</mapper>
