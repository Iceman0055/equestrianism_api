<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- user_info表 -->
<mapper namespace="com.equestrianism.api.dao.HostInfoMapper">

    <!--返回Map-->
    <resultMap id="BaseResultMap" type="com.equestrianism.api.model.po.HostInfoEntity">
        <id column="host_id" property="hostId" jdbcType="VARCHAR"/>
        <result column="host_name" property="hostName" jdbcType="VARCHAR"/>
        <result column="sex" property="sex" jdbcType="INTEGER"/>
        <result column="occupation" property="occupation" jdbcType="VARCHAR"/>
        <result column="contact_way" property="contactWay" jdbcType="VARCHAR"/>
        <result column="address" property="address" jdbcType="VARCHAR"/>
        <result column="horse_id" property="horseId" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="delete_flag" property="deleteFlag" jdbcType="INTEGER"/>
        <result column="data_version" property="dataVersion" jdbcType="INTEGER"/>
        <result column="create_date" property="createDate" jdbcType="TIMESTAMP"/>
        <result column="update_date" property="updateDate" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--基本的sql查询字段 公共引用...-->
    <sql id="Base_Column_List">
        host_id, host_name, sex, occupation, contact_way, address, horse_id, status, delete_flag, data_version, create_date, update_date
    </sql>


    <insert id="insert" parameterType="com.equestrianism.api.model.po.HostInfoEntity">
        INSERT INTO
            host_info (
                host_id,
                host_name,
                sex,
                occupation,
                contact_way,
                address,
                horse_id,
                status,
                delete_flag,
                data_version,
                create_date
            ) VALUES (
                #{hostId, jdbcType=VARCHAR},
                #{hostName, jdbcType=VARCHAR},
                #{sex, jdbcType=INTEGER},
                #{occupation, jdbcType=VARCHAR},
                #{contactWay, jdbcType=VARCHAR},
                #{address, jdbcType=VARCHAR},
                #{horseId, jdbcType=VARCHAR},
                1,
                0,
                1,
                now()
            )
    </insert>

    <update id="updateBySelective" parameterType="com.equestrianism.api.model.po.HostInfoEntity">
        UPDATE
            host_info
        <set>
            <if test="hostName != null and hostName != ''">
                host_name = #{hostName, jdbcType=VARCHAR},
            </if>
            <if test="sex != null">
                sex = #{sex, jdbcType=INTEGER},
            </if>
            <if test="occupation != null and occupation != ''">
                occupation = #{occupation, jdbcType=VARCHAR},
            </if>
            <if test="contactWay != null and contactWay != ''">
                contact_way = #{contactWay, jdbcType=VARCHAR},
            </if>
            <if test="address != null and address != ''">
                address = #{address, jdbcType=VARCHAR},
            </if>
            <if test="horseId != null and horseId != ''">
                horse_id = #{horseId, jdbcType=VARCHAR},
            </if>
            <if test="status != null">
                status = #{status, jdbcType=INTEGER},
            </if>
            <if test="deleteFlag != null">
                delete_flag = #{deleteFlag, jdbcType=INTEGER}
            </if>
        </set>
        WHERE
            host_id = #{hostId, jdbcType=VARCHAR}
    </update>

    <select id="selectHostListByPage" parameterType="com.equestrianism.api.model.vo.host_info.HostInfoListVO"
            resultType="com.equestrianism.api.model.model.HostInfoListModel">
        SELECT
            a.host_id         hostId,
            a.host_name       hostName,
            a.sex             sex,
            a.occupation      occupation,
            a.contact_way     contactWay,
            a.address         address,
            b.horse_name      horseName
        FROM
            host_info a
        LEFT JOIN
            horse_info b
        ON
            a.horse_id = b.horse_id
        WHERE
            a.delete_flag = 0
        AND
            a.status = 1
        <if test="hostName != null and hostName != ''">
            AND
                a.host_name like CONCAT('%', #{hostName, jdbcType=VARCHAR}, '%')
        </if>
        <if test="contactWay != null and contactWay != ''">
            AND
                a.contact_way like CONCAT('%', #{contactWay, jdbcType=VARCHAR}, '%')
        </if>
        limit #{beginIndex}, #{pageRecorders}
    </select>

    <select id="countHostListByPage" parameterType="com.equestrianism.api.model.vo.host_info.HostInfoListVO"
            resultType="java.lang.Integer">
        SELECT
            count(1)
        FROM
            host_info a
        LEFT JOIN
            horse_info b
        ON
            a.horse_id = b.horse_id
        WHERE
            a.delete_flag = 0
        AND
            a.status = 1
        <if test="hostName != null and hostName != ''">
            AND
            a.host_name like CONCAT('%', #{hostName, jdbcType=VARCHAR}, '%')
        </if>
        <if test="contactWay != null and contactWay != ''">
            AND
            a.contact_way like CONCAT('%', #{contactWay, jdbcType=VARCHAR}, '%')
        </if>
    </select>

    <select id="selectByPrimaryKey" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT
            <include refid="Base_Column_List"/>
        FROM
            host_info
        WHERE
            host_id = #{hostId, jdbcType=VARCHAR}
    </select>

    <select id="selectByResume" parameterType="java.lang.String" resultType="com.equestrianism.api.model.bo.HostInfoDetailBO">
        SELECT
            host_name     hostName,
            sex           sex,
            occupation    occupation,
            contact_way   contactWay
        FROM
            host_info
        WHERE
            delete_flag = 0
        AND
            status = 1
        AND
            horse_id = #{horseId, jdbcType=VARCHAR}
    </select>

</mapper>
